package org.jug.nicaragua.picoweb;

/**
 * The entry point of the Spring Boot application.
 */
// @SpringBootApplication
public class Application {

  public static void main(String[] args) {
    // SpringApplication.run(Application.class, args);

    // Create Spring application context
    // ClassPathXmlApplicationContext ctx = new
    // ClassPathXmlApplicationContext("classpath:/WEB-INF/web.xml");

    // Get service from context. (service's dependency (ProductDAO) is autowired in
    // ProductService)

    // PacienteService productService = ctx.getBean(PacienteService.class);
  }

}
